**Checkbox** — teal checkbox for consent and multi-select, with optional description.

```jsx
<Checkbox label="I consent to be contacted about my appointment"
  description="We'll only use your details for your care." />
```

Works controlled (`checked` + `onChange`) or uncontrolled (`defaultChecked`).
