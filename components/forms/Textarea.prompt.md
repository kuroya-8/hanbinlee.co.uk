**Textarea** — multi-line field for symptoms, reason for referral, or a message.

```jsx
<Textarea label="Reason for appointment" rows={5} hint="Briefly describe your symptoms" />
```

Same `label`/`hint`/`error` API as Input; `rows` sets initial height, user-resizable vertically.
